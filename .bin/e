#!/usr/bin/env rc

pgrep emacs || emacs --daemon

if (~ $1 -) {
  shift
  buffer=`{mktemp} {
    cat > $buffer
    emacsclient -c $buffer $*
    cat $buffer
    rm $buffer >[2=1] >/dev/null
  }

  return
} else {
  emacsclient -c $*
}